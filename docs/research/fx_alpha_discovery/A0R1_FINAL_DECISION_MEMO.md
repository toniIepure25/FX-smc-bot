# Gate A0R1 Final Decision Memo

Gate: `A0R1_CLEAN_ROOM_AND_EMPIRICAL_DISCOVERY_V1`

Decision: `BLOCKED_BY_A0R1_MARKET_DATA_ACQUISITION`

The predecessor A.0 market-data blocker remains valid and unchanged. A0R1
resolved only the missing clean-room configuration by bootstrapping
`FX_A0R1_CLEAN_ROOM_V1`.

The hypothesis registry and search-space freeze remained unchanged. The 1200
candidate-equivalent trial plan was registered before any provider request.

The discovery request plan was frozen before provider access. A single
authorized Dukascopy probe for `EURUSD`, `2010-01-04`, bid side succeeded and
was stored only in the clean room. The full 2010-2014 nine-instrument discovery
dataset was not acquired or certified, so no features, strategy outcomes,
shortlist, validation, replication or future candidate exists.

Every empirical configuration was registered before its outcome. No trial
outcome was calculated, so no unsuccessful, invalid or technically failed trial
was removed.

Every unsuccessful, invalid and technically failed trial remained in the trial
registry.

No strategy held a position through the frozen rollover boundary. No rate or
financing series was accessed. Development, internal confirmation, external
validation and independent replication were accessed only in preregistered
order; for this blocked run, none of those empirical stages was reached. The
2023-2025 interval remained inaccessible. NZD and NZDUSD remained inaccessible.

No discovery-stage positive result exists and no alpha is described as
confirmed. A discovery-stage positive result was not described as confirmed
alpha. Any selected candidate remains unconfirmed until the frozen future
protocol is completed. No result authorizes live-capital deployment. No result
authorizes paper trading, a pull request or a merge.
