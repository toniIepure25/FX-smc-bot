# Gate F0-RP-E2E-R-USDSR-LPA Predecessor Inheritance

Status: `PREDECESSOR_BLOCK_PRESERVED_NOT_REVERSED`.

The predecessor decision remains `BLOCKED_BY_USD_PUBLICATION_AVAILABILITY`. Its schema descriptors, reconciliation, availability assessment, audits, manifest and final memo retain their original LF-normalized hashes.

The predecessor correctly established two explicitly versioned EFFR schemas and did not implement `NY_FED_EFFR_V3`. It emitted no numerical row to the parser, created no `RateVersion`, generated no economic outcome and invented no legacy publication clock time.

This gate addresses the representation contract prospectively. It does not reclassify the predecessor blocker as a false alarm and does not rewrite its evidence.
