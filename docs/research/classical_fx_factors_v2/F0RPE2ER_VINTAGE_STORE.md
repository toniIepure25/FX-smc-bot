# F0-RP-E2E-R Vintage Store

`F0RPE2ER_RATE_VINTAGE_STORE_V3` is a new-database-only, append-only SQLite schema. Exact requests bind snapshots and versions; a passing firewall certification is required before snapshot persistence. Explicit as-of timestamps and frozen version membership prevent revised values from leaking backward.
