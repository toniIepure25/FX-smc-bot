# Gate F0-RP-E2E-R Clean Room

The V2 clean room was created directly from the preregistered path template
`<repository-parent>/FX-smc-bot-local-data/f0rpe2er_v2`. It did not exist before
the gate, was initially empty, is outside the repository, and is not a symlink
or junction.

No old root was inspected or reused. Git stores only a one-way path identity
hash and never the absolute local path.
