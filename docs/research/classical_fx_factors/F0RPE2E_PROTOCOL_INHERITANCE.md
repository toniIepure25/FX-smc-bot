# Gate F0-RP-E2E Scientific Protocol Inheritance

## Status

`PASS`

Lock `F0RPE2E_SCIENTIFIC_PROTOCOL_INHERITANCE_V1` pins the exact current
factor configuration, factor families, portfolio construction, costs and
accounting, benchmarks, estimands and inference, selection and replication
rules, and the outcome-blind F0-RP rate-provenance amendment.

This gate may implement official source adapters, strict parsing, immutable
source snapshots, point-in-time rate persistence, rate and market alignment,
recovery and empirical orchestration, tests, and aggregate audit artifacts.
It may not change factor equations or parameters, portfolio construction,
cost assumptions, benchmarks, inference, eligibility thresholds, temporal
partitions, or the scientific meaning of the frozen protocol.

The exact LF-normalized SHA-256 values are recorded in
`results/gate_f0rpe2e/protocol_inheritance_lock.json`. The lock was created
before any market or official-rate observation access for this gate.
