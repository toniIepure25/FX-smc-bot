# Q.0-R Protocol Inheritance

Lock ID: `Q0R_Q0_PROTOCOL_INHERITANCE_V1`.

Q.0-R inherits the complete committed Q.0 scientific protocol because Q.0
generated neither development nor replication outcomes. Six source freeze
artifacts are locked by SHA-256, alongside the Q.0 canonical candidate-family,
feature-order, and protocol hashes.

The candidate family remains exactly four deterministic direction inversions and
one multinomial elastic-net polarity selector. The model retains the nine-product
grid formed by `C` values 0.01, 0.1, and 1.0 and `l1_ratio` values 0.0, 0.5, and
1.0. Hyperparameter selection remains multinomial log loss only. ORIGINAL and
INVERSE each require probability at least 0.55; every other prediction abstains.

The purge remains 2,680 minutes and the embargo remains five FX trading days.
All estimands, benchmarks, development eligibility checks, Tier A/B replication
criteria, and mechanical ranking rules are unchanged.

The only permitted differences are the clean data root, capability-based safe
I/O, permanent quarantine of the historical disqualified interval, and a new
future prospective holdout.
